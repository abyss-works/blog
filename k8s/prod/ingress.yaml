apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blog
  namespace: blog-prod
  labels:
    project: blog
    environment: prod
spec:
  ingressClassName: nginx
  rules:
  - host: "blog.abyssworks.dev"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blog
            port:
              number: 80
